<!DOCTYPE html>
<html lang="es" data-theme="corporate">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Sora:wght@600;700&display=swap"
      rel="stylesheet"
    />

    <title>{{ title || 'proyecto-catedra-afe' }}</title>

    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif

    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>

  <body class="auth-body min-h-screen">
    <nav class="auth-navbar navbar px-4 backdrop-blur-sm md:px-6">
      <div class="flex-1">
        <a class="btn btn-ghost auth-brand text-lg font-bold" href="/">
          <span class="badge auth-brand-badge">AFE</span>
          <span class="ml-2">proyecto-catedra-afe</span>
        </a>
      </div>

      <div class="flex-none items-center gap-2">
        <button id="themeToggle" type="button" class="btn btn-sm auth-ghost-btn app-theme-toggle">
          <span class="theme-toggle-icon" aria-hidden="true"></span>
          <span class="theme-toggle-label">Theme</span>
        </button>
      </div>
    </nav>

    <main class="auth-main p-4 md:p-6 lg:p-10">
      <div class="auth-glow auth-glow-a"></div>
      <div class="auth-glow auth-glow-b"></div>

      <div class="auth-shell mx-auto grid w-full max-w-6xl gap-6 rounded-3xl p-2 md:grid-cols-[1.1fr_1fr] md:p-4">
        <section class="auth-showcase hidden rounded-2xl p-8 md:flex md:flex-col md:justify-between">
          <div>
            <span class="badge auth-kicker-badge">Portal Institucional</span>
            <h1 class="auth-showcase-title mt-5 text-3xl leading-tight">
              Plataforma segura para operar procesos internos.
            </h1>
            <p class="auth-showcase-copy mt-4 max-w-md text-sm">
              Accede con tu cuenta autorizada para gestionar usuarios, roles, permisos y bitácora.
            </p>
          </div>

          <div class="auth-showcase-list grid gap-3 text-sm">
            <div class="auth-feature-pill rounded-xl p-3">
              Seguridad por roles y permisos
            </div>
            <div class="auth-feature-pill rounded-xl p-3">
              Auditoría de eventos críticos
            </div>
            <div class="auth-feature-pill rounded-xl p-3">
              Administración centralizada
            </div>
          </div>
        </section>

        <section class="auth-form-wrap rounded-2xl p-5 md:p-7">
          {{{ await $slots.main() }}}
        </section>
      </div>
    </main>
  </body>
</html>