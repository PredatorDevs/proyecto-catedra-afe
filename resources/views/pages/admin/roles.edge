@component('../views/components/layouts/app', { title: 'Admin Roles - proyecto-catedra-afe' })
  <div class="space-y-4">
    <div class="card bg-base-100 shadow">
      <div class="card-body">
        <h1 class="text-2xl font-bold">Roles</h1>
        <p>Listado básico de roles y permisos asignados.</p>
      </div>
    </div>

    <div class="card bg-base-100 shadow">
      <div class="card-body overflow-x-auto">
        <table class="table table-zebra">
          <thead>
            <tr>
              <th>ID</th>
              <th>Slug</th>
              <th>Nombre</th>
              <th>Permisos</th>
            </tr>
          </thead>
          <tbody>
            @if(roles.length === 0)
              <tr>
                <td colspan="4" class="text-center text-base-content/70">Sin roles registrados.</td>
              </tr>
            @endif

            @each(role in roles)
              <tr>
                <td>{{ role.id }}</td>
                <td>{{ role.slug }}</td>
                <td>{{ role.name }}</td>
                <td>
                  <div class="flex flex-wrap gap-1">
                    @if(role.permissions.length === 0)
                      <span class="badge badge-ghost">sin permisos</span>
                    @endif
                    @each(permission in role.permissions)
                      <span class="badge badge-outline">{{ permission.slug }}</span>
                    @end
                  </div>
                </td>
              </tr>
            @end
          </tbody>
        </table>
      </div>
    </div>
  </div>
@end
